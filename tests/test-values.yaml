# By default we pull these values: https://repo1.dso.mil/big-bang/bigbang/-/blob/master/tests/disable-core-values.yaml
kyvernoPolicies:
  values:
    bbtests:
      enabled: false
kyverno:
  values:
    bbtests:
      enabled: false

addons:
  anchoreEnterprise:
    enabled: true