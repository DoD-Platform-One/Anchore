networkPolicies:
  enabled: true

bbtests:
  enabled: true
  # ENV injects delay into package CI until a better solution is in place
  scripts:
    envs:
      PACKAGE_CI: "true"

anchoreGlobal:
  saml:
    secret: ci-testing-only

anchoreAnalyzer:
  replicaCount: 1
